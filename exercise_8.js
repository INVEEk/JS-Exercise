/*  6. Create two variables called width and height . Then, create a new variable that holds the area.
Print it to the console
 */

const height = 30;
const width = 20;
const area = height * width;
console.log(area)