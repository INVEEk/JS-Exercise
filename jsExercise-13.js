/*

Delete the else statement and adjust the code to make sure that the output is the same as before.
Remember that you can give a default value to a variable when declaring it a.


const age = 21;
let isAdult;
if(age >= 18) {
    is Adult = true;
} else {
    isAdult = false;
}

alert(isAdult)
 */
const age = 21;
let isAdult = 18;
console.log(age > isAdult)
